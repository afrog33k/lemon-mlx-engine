diff --git a/mlx/backend/rocm/gemms/hipblaslt_gemm.cpp b/mlx/backend/rocm/gemms/hipblaslt_gemm.cpp
index 4b53db54..5f8d2c41 100644
--- a/mlx/backend/rocm/gemms/hipblaslt_gemm.cpp
+++ b/mlx/backend/rocm/gemms/hipblaslt_gemm.cpp
@@ -493,13 +493,16 @@ void hipblaslt_gemm(
   hipblasOperation_t op_a = to_hipblas_op(transpose_b);
   hipblasOperation_t op_b = to_hipblas_op(transpose_a);
 
-  static bool dbg = []{
-    fprintf(stderr, "[hipBLASLt] first call\n");
-    return true;
-  }();
-  (void)dbg;
-  fprintf(stderr, "[hipBLASLt] M=%d N=%d K=%d ta=%d tb=%d lda=%d ldb=%d ldc=%d\n",
-      M, N, K, (int)transpose_a, (int)transpose_b, lda, ldb, ldc);
-
+  static bool debug = std::getenv("MLX_ROCM_HIPBLASLT_DEBUG") != nullptr;
+  if (debug) {
+    static bool first = []{
+      fprintf(stderr, "[hipBLASLt] first call\n");
+      return true;
+    }();
+    (void)first;
+    fprintf(stderr, "[hipBLASLt] M=%d N=%d K=%d ta=%d tb=%d lda=%d ldb=%d ldc=%d\n",
+        M, N, K, (int)transpose_a, (int)transpose_b, lda, ldb, ldc);
+  }
+
   const void* a_ptr = gpu_ptr<void>(a);
   const void* b_ptr = gpu_ptr<void>(b);
   void* c_ptr = gpu_ptr<void>(c);
